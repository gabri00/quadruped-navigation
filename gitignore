# Cartelle generate da colcon build — non vanno mai committate
# Vengono ricreate automaticamente ogni volta che esegui "colcon build"
build/
install/
log/

# File di configurazione locale di colcon
.colcon_install_layout

# Cache Python
__pycache__/
*.pyc
*.pyo

# File di sistema macOS
.DS_Store

# File di sistema Windows
Thumbs.db
